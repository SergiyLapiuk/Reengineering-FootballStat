@model FootBallStat.PlayersInMatch

@{
    ViewData["Title"] = "Формування заявки на матч";
}

<h1>Формування заявки на матч</h1>

<h4>Гравець в матчі</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PlayerId" class="control-label"></label>
                <select asp-for="PlayerId" class ="form-control" asp-items="ViewBag.PlayerId"></select>
            </div>
           
            <div class="form-group">
                <label asp-for="PlayerGoals" class="control-label"></label>
                <input asp-for="PlayerGoals" class="form-control" />
                <span asp-validation-for="PlayerGoals" class="text-danger"></span>
            </div>

            <input type="hidden" value="@ViewBag.MatchId" name="MatchId"/>

            <div class="form-group">
                <input type="submit" value="Додати гравця" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
         <p class="text-danger">@ViewData["ErrorMessage"]</p>
</div> 

@*<div>
    <a asp-action="Index">Back to List</a>
</div>*@

<p>
     @Html.ActionLink("Повернутись до списку", "Index", new { id = @ViewBag.MatchId, team1Id = @ViewBag.MatchTeam1Id, team2Id = @ViewBag.MatchTeam2Id, championshipId = @ViewBag.MatchChampionshipId,
     date = @ViewBag.MatchDate}, new { @class = "btn btn-success" })
</p>

@*<div>
    <a asp-action="Index"
    asp-route-id="@Model.MatchId"
    asp-route-team1id="@Model.Match.Team1Id"
    asp-route-team2id="@Model.Match.Team2Id"
    asp-route-championshipid="@Model.Match.ChampionshipId"
    asp-route-date="@Model.Match.Date">Back to List</a>
</div>*@

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
